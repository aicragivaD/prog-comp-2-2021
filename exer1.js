let exercicio1 = () => {
    //objetos
    let vetorEmpresa = []
    for (let i=0; i<4; i++){
        let objeto = {
            codigo: Number(prompt(`Informe o codigo do vendedor`)), //codigo do vendedor
            venda: prompt(`Informe o total de vendas`), //cadastrar a venda
            Mes: prompt(`Informe o mes`) //vendas do funcionario em determinado mes
        }
    }
    //cadastro vendedor
    let maisVendeu = 0
    for (let i=0; i<4; i++){
        if (codigo){
            
        }

    }
}